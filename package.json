{
  "name": "bribox-supabase-v.2.0",
  "version": "1.0.0",
  "description": "BRIBOX KANPUS Application Report web app.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwindcss -i ./style.css -o ./dist/style.css --minify",
    "watch": "tailwindcss -i ./style.css -o ./dist/style.css --watch"
  },
  "author": "BangAtuy",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  }
}
